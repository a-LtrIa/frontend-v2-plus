整理好了。下面这份可以直接作为项目的 `README.md` / `PROJECT_DESIGN.md` 初稿。

````md
# MAGI — 明日方舟抽卡决策系统

> 基于概率模拟、多目标决策与多数投票机制的明日方舟抽卡辅助系统  
> **核心思想：不是告诉玩家“该不该抽”，而是让三个不同目标的决策系统分别审视这次抽卡。**

---

## 1. 项目概述

作为明日方舟玩家，在面对一个卡池时，玩家往往同时存在三种不同的决策倾向：

- **大头**：理性考虑资源、概率和目标达成
- **小头**：考虑自己对角色的喜爱程度
- **兔头**：考虑角色强度、机制以及对自身队伍的价值

这三种倾向经常发生冲突。

例如：

> 当前拥有 180 抽，本期限定角色非常喜欢，同时陪跑六星强度很高，但抽完可能导致未来资源不足。

此时：

- 大头可能认为“不应该抽”
- 小头可能认为“必须抽”
- 兔头可能认为“值得抽”

因此，本项目借鉴《新世纪福音战士》中的 **MAGI 三机决策模式**，建立三个具有不同目标函数的决策模块，并通过投票产生最终建议。

---

# 2. 核心设计理念

## 2.1 MAGI 不是三个 LLM Agent

项目不将三个 MAGI 设计成三个独立的大语言模型。

而是：

> **三个确定性的 Decision Engine（决策引擎）。**

三个模块分别使用数学模型、评分模型和规则系统进行计算。

```text
                    玩家
                     │
                     ↓
              玩家数据 / 当前状态
                     │
                     ↓
               卡池规则引擎
                     │
                     ↓
                抽卡概率模拟
                     │
        ┌────────────┼────────────┐
        ↓            ↓            ↓
      大头          小头          兔头
    理性决策      角色偏好      强度价值
        │            │            │
        └────────────┼────────────┘
                     ↓
                 MAGI 投票
                     ↓
             抽 / 不抽 / 抽多少
````

---

# 3. 为什么核心不使用 LLM

如果网站拥有大量用户，让每次抽卡决策都调用 LLM 会产生：

* 高 API 成本
* 高延迟
* 并发压力
* 决策结果不稳定
* 难以验证计算正确性

因此：

> **核心决策必须完全脱离 LLM。**

所有核心计算应该由：

* 概率计算
* 蒙特卡洛模拟
* 动态规划
* 数学评分函数
* 规则引擎
* 投票系统

完成。

---

# 4. LLM 的定位

未来可以加入 LLM，但只作为可选的解释层。

例如 MAGI 已经计算出：

```text
大头：不抽
小头：抽
兔头：抽

最终：抽
```

用户点击：

> 为什么？

再将结构化结果交给 LLM，让 LLM解释三个 MAGI 为什么这么判断。

因此：

```text
核心计算
    ↓
确定性算法

解释
    ↓
可选 LLM
```

而不是：

```text
用户请求
    ↓
LLM
    ↓
决定抽不抽
```

---

# 5. 系统整体架构

```text
┌─────────────────────────────────────────────┐
│                   前端                       │
│                                             │
│  攒抽计算器 / 卡池页面 / 玩家设置 / MAGI UI │
└──────────────────────┬──────────────────────┘
                       ↓
┌─────────────────────────────────────────────┐
│                  API 层                      │
└──────────────────────┬──────────────────────┘
                       ↓
┌─────────────────────────────────────────────┐
│                决策系统                      │
│                                             │
│  Probability Engine                         │
│  Target Engine                              │
│  Big Head Engine                            │
│  Small Head Engine                          │
│  Rabbit Head Engine                         │
│  MAGI Voting Engine                         │
└──────────────────────┬──────────────────────┘
                       ↓
┌─────────────────────────────────────────────┐
│                  数据层                      │
│                                             │
│  卡池数据库                                  │
│  干员数据库                                  │
│  卡池规则                                    │
│  玩家资源                                    │
│  玩家 BOX                                     │
└─────────────────────────────────────────────┘
```

---

# 6. 玩家数据系统

玩家数据主要分为四类：

## 6.1 资源状态

记录玩家当前拥有的抽卡资源。

例如：

* 合成玉
* 至纯源石
* 单抽券
* 十连寻访凭证
* 其他特殊抽卡资源
* 当前可用抽数

以及：

* 当前六星保底计数
* 当前卡池特殊计数
* 井 / 数据契约等状态

---

# 7. 资源安全线

玩家可以设置：

> **达成目标后希望至少剩余多少抽。**

例如：

```text
当前资源：180 抽

目标：
获得当前限定

资源安全线：
60 抽
```

如果模拟结果显示：

```text
完成目标后预计剩余：
80 抽
```

则满足安全线。

如果：

```text
完成目标后预计剩余：
40 抽
```

则突破安全线。

这个参数主要影响：

> **大头的风险判断。**

---

# 8. 玩家抽卡目标系统

玩家可以同时设置多个目标。

目标可以通过拖动卡片进行排序。

排序代表：

> **目标优先级。**

---

## 8.1 当前目标类型

### 卡池毕业

获得当前卡池中希望获得的全部目标干员。

---

### 获得限定

获得当前卡池中的限定干员。

例如：

> 300 抽井。

---

### 获得陪跑六星

获得当前卡池中的陪跑六星。

---

### 获得五星

获得当前卡池中目标五星。

---

### 获得特定往期限定

分为：

* 未降井限定
* 已降井限定

分别根据当前规则计算获取成本。

---

### 补充未获得的常驻六星

玩家可以手动指定：

> 自己缺少的某个常驻六星。

该目标属于可选目标。

---

# 9. 目标优先级

建议将目标划分为：

```text
S级：必须完成
A级：非常希望完成
B级：有余力完成
C级：顺便获得
```

也可以通过拖动排序直接形成优先级。

例如：

```text
1. 当前限定       S
2. 陪跑六星       A
3. 常驻六星       B
4. 五星           C
```

这样系统可以处理：

> 多目标之间的冲突。

---

# 10. 卡池规则引擎

卡池规则必须独立于 MAGI。

原因：

> 不同卡池的概率、保底、井、计数继承、限定规则存在差异。

因此不能把规则直接写进三个 MAGI。

建议设计成：

```text
Banner
├── banner_type
├── start_time
├── end_time
├── operators
├── rarity_rates
├── pity_rules
├── target_rules
├── spark_rules
├── currency_rules
└── special_rules
```

---

# 11. 概率计算引擎

负责计算：

* 单抽概率
* 六星概率递增
* UP 概率
* 目标角色概率
* 保底
* 井
* 特殊卡池规则

输出：

```text
P(目标在 N 抽内获得)
P(至少获得一个目标)
P(完成目标集合)
E(获得目标所需抽数)
```

---

# 12. 抽卡模拟

对于复杂卡池，可以使用 Monte Carlo Simulation。

例如：

```text
模拟次数：100000

第 1 次模拟 → 83 抽获得目标
第 2 次模拟 → 121 抽获得目标
第 3 次模拟 → 67 抽获得目标
...
```

最终获得：

```text
50 抽内获得：63.4%
100 抽内获得：87.2%
150 抽内获得：96.8%
200 抽内获得：99.4%
```

---

# 13. 缓存与高并发设计

由于很多玩家可能进行相似计算，可以对概率结果进行缓存。

例如：

```text
卡池
+
目标
+
当前保底
+
抽数
```

对应一组概率结果。

对于固定卡池，可以预计算：

```text
0 ～ 300 抽
×
不同保底状态
×
不同目标
```

这样普通用户访问时可以直接读取结果。

目标：

> **尽可能减少实时模拟。**

---

# 14. 大头：理性决策引擎

## 核心问题

> “从资源和概率角度来看，现在抽卡是否值得？”

大头主要考虑：

* 当前资源
* 当前抽数
* 卡池概率
* 保底
* 目标优先级
* 目标完成概率
* 达成目标所需抽数
* 资源安全线
* 卡池是否复刻
* 距离进入中坚等未来获取途径的时间
* 是否存在井
* 是否降井

---

# 15. 大头的核心思想

大头不考虑：

> “我喜欢不喜欢这个角色。”

而考虑：

> “用当前资源完成目标是否合理。”

可以抽象为：

```text
目标收益
-
资源成本
-
失败风险
-
机会成本
=
理性价值
```

---

# 16. 小头：角色偏好决策引擎

## 核心问题

> “我到底有多喜欢这个角色？”

角色偏好不应该简单使用一个 1～5 分。

应该拆成多个维度。

---

## 16.1 角色偏好维度

```text
立绘
小人
剧情
人设
配音
画师
二创
特殊 XP
收藏价值
```

每项可以由玩家评分。

例如：

```text
立绘：9
小人：8
剧情：10
人设：10
配音：7
画师：9
二创：6
特殊 XP：10
收藏价值：8
```

---

# 17. 个性化权重

不同玩家对于不同维度的重视程度不同。

因此允许玩家设置权重。

例如：

```text
剧情 × 1.5
人设 × 1.5
立绘 × 1.0
配音 × 0.5
特殊 XP × 2.0
```

最终计算：

> **角色主观效用值。**

---

# 18. 图鉴党

“喜欢角色”和“想收集角色”应该分开。

例如：

```text
角色厨力：4/10
收藏价值：10/10
```

表示：

> 不特别喜欢这个角色，但希望补齐图鉴。

因此：

> 收藏价值应该成为小头独立维度。

---

# 19. 兔头：强度决策引擎

## 核心问题

> “这个角色对我的游戏体验到底有多大价值？”

不能简单定义为：

> 角色强度评分。

而应该考虑：

> **角色强度 × 玩家实际需求。**

---

# 20. 兔头评价维度

```text
挂机
开荒
大数字
特殊机制
体系缺口
肉鸽
特定强度
职业队
未来潜力
泛用性
```

---

# 21. 玩家 BOX

兔头必须考虑玩家已有干员。

因为：

> 同一个角色对不同玩家的价值不同。

例如：

```text
玩家 A：
先锋体系已经完整

玩家 B：
先锋体系存在明显缺口
```

同一个先锋角色：

```text
玩家 A：价值 6/10
玩家 B：价值 10/10
```

因此需要引入：

> **边际战力价值。**

---

# 22. 体系缺口

玩家可以主动指定：

```text
我是先锋队玩家
我是重装队玩家
我是狙击队玩家
我是术师队玩家
……
```

或者让系统根据 BOX 自动分析：

```text
职业缺口
定位缺口
机制缺口
体系缺口
```

从而修正兔头评价。

---

# 23. 三个 MAGI 的输出

三个决策引擎不应该只输出：

> 抽 / 不抽。

建议输出：

```text
decision
score
confidence
recommended_pulls
stop_condition
reason_codes
```

例如：

```text
大头：

决策：不抽
评分：42
建议：停止
原因：
- 目标概率不足
- 会突破资源安全线
```

---

# 24. MAGI 投票机制

基础机制：

> **三选二多数决。**

例如：

```text
大头：❌
小头：✅
兔头：✅

2 : 1

MAGI：
建议抽卡
```

---

# 25. 高风险决策

普通抽卡：

> 2/3 多数即可。

但对于重大资源消耗：

> 需要更高投票门槛。

例如：

```text
可能突破资源安全线
一次消耗超过 100 抽
可能导致资源归零
```

可以要求：

> **3/3 同意。**

---

# 26. 极端决策

如果决策可能：

> 将玩家资源消耗至接近 0。

则：

```text
MAGI：
建议梭哈

↓

玩家确认

↓

执行模拟
```

即：

> MAGI 负责建议，最终资源风险由玩家确认。

---

# 27. MAGI 的最终输出

最终页面可以展示：

```text
━━━━━━━━━━━━━━━━━━━━━━
          MAGI
━━━━━━━━━━━━━━━━━━━━━━

当前资源
180 抽

目标
① 当前限定
② 陪跑六星
③ 五星

安全线
60 抽

━━━━━━━━━━━━━━━━━━━━━━

🧠 大头
❌ 不抽
42 / 100

❤️ 小头
✅ 抽
91 / 100

🐇 兔头
✅ 抽
86 / 100

━━━━━━━━━━━━━━━━━━━━━━

        2 : 1

🔥 MAGI 建议：抽卡

建议：
最多抽 100 抽

停止条件：
获得限定后停止

━━━━━━━━━━━━━━━━━━━━━━
```

---

# 28. 决策不是“抽 / 不抽”二元问题

MAGI 最终应该逐渐发展成：

```text
不抽
│
├── 等待
├── 抽 10
├── 抽 20
├── 抽 50
├── 抽到目标停止
├── 抽到六星停止
└── 梭哈
```

因此最终目标是：

> **给出抽卡策略，而不是简单的二元判断。**

---

# 29. 玩家历史记录

每次决策记录：

```text
时间
卡池
当时资源
目标
安全线
三个 MAGI 判断
最终决策
玩家实际行为
抽卡结果
```

例如：

```text
2026-08-12

资源：180
目标：限定
安全线：60

大头：停止
小头：抽
兔头：抽

MAGI：抽

玩家实际：抽了 120
结果：87 抽获得目标
```

这些数据未来可以用于：

* 决策复盘
* 玩家行为分析
* 个性化模型
* 机器学习实验

---

# 30. 未来机器学习方向

项目第一版不需要机器学习。

先建立可靠的：

> 概率模型 + 决策模型。

之后可以逐渐加入机器学习。

---

## 30.1 玩家行为预测

根据历史行为预测：

> 玩家实际会不会遵循 MAGI。

---

## 30.2 玩家偏好学习

根据玩家历史评分和抽卡行为：

> 自动学习角色偏好。

---

## 30.3 个性化权重

学习：

```text
大头权重
小头权重
兔头权重
```

使 MAGI 越来越符合玩家本人。

---

## 30.4 强化学习

将抽卡环境视为：

> Sequential Decision Environment

状态：

```text
资源
保底
目标
卡池
未来资源
```

行动：

```text
抽
停止
抽 N 次
```

奖励：

```text
目标完成收益
+
角色偏好收益
+
强度收益
-
资源消耗
```

从而研究：

> AI 是否能够学习出合理的抽卡策略。

---

# 31. LLM 的未来定位

LLM 不参与核心计算。

未来可以作为：

## MAGI 解说员

负责：

* 解释决策
* 总结三个 MAGI 的观点
* 进行自然语言问答
* 生成决策报告

例如：

> “为什么小头和兔头都建议抽？”

LLM读取结构化数据后进行解释。

---

# 32. CKTI 抽卡人格

暂时不纳入核心系统。

未来可以作为独立娱乐功能。

原则：

> **不能影响核心抽卡决策。**

如果加入，也应该基于：

* 玩家历史决策
* MAGI 投票记录
* 玩家资源管理行为
* 玩家角色偏好
* 玩家实际抽卡行为

进行分析。

---

# 33. 第一阶段 MVP

第一版不要追求复杂 AI。

只实现：

```text
① 卡池数据库
② 卡池规则引擎
③ 攒抽计算器
④ 抽卡概率模拟
⑤ 玩家资源输入
⑥ 抽卡目标
⑦ 目标优先级
⑧ 资源安全线
⑨ 大头
⑩ 小头
⑪ 兔头
⑫ MAGI 投票
```

实现：

> **完整的一次抽卡决策流程。**

---

# 34. 第二阶段

增加：

```text
玩家 BOX
角色强度数据
角色偏好数据
体系分析
历史决策记录
```

使 MAGI 从：

> 通用决策

变成：

> **个性化决策。**

---

# 35. 第三阶段

增加：

```text
未来卡池规划
长期资源规划
多卡池竞争
机会成本
动态抽卡策略
```

此时“大头”从：

> 当前卡池决策

升级为：

> **长期资源优化。**

---

# 36. 第四阶段

加入：

```text
玩家行为学习
个性化权重
机器学习模型
强化学习实验
```

使系统逐渐从：

> 人工设计规则

转变为：

> **数据驱动的个性化决策系统。**

---

# 37. 项目的核心研究问题

这个项目最终可以抽象为一个机器学习 / 决策科学问题：

> **当一个决策同时受到资源约束、个人偏好和客观价值影响时，多目标决策系统能否通过多个具有不同效用函数的决策模块进行投票，产生比单一目标优化更符合玩家需求的策略？**

明日方舟只是这个问题的实验环境。

---

# 38. 核心价值

这个项目并不是：

> “做一个明日方舟抽卡概率计算器。”

真正的核心是：

> **建立一个面向不确定性资源决策的多目标决策实验平台。**

明日方舟提供：

* 概率
* 保底
* 有限资源
* 多个目标
* 角色价值差异
* 玩家主观偏好
* 长期资源约束

MAGI 提供：

* 多目标决策
* 冲突
* 投票
* 风险控制
* 个性化决策

最终可以进一步扩展到其他具有类似结构的决策问题。

---

# 39. 项目一句话定义

> **MAGI 是一个基于明日方舟抽卡环境的多目标个性化决策系统，通过“理性资源、角色偏好、游戏强度”三个独立决策引擎进行投票，为玩家提供可解释的抽卡策略建议。**

---

# 40. 开发原则

### 原则 1

**先规则，后 AI。**

### 原则 2

**先确定性计算，后 LLM。**

### 原则 3

**先解决“该怎么计算”，再解决“怎么展示”。**

### 原则 4

**MAGI 的每个判断都必须可解释。**

### 原则 5

**概率计算与决策逻辑必须解耦。**

### 原则 6

**卡池规则必须独立建模，不能写死在 MAGI 中。**

### 原则 7

**玩家最终拥有决策权，MAGI 只提供建议。**

---

```

我建议下一步就不要继续扩展功能了，而是直接做 **“MAGI 决策数学模型 v0.1”**：把大头、小头、兔头分别需要哪些输入、怎么计算分数、什么时候判定“抽/不抽”，以及三者如何从分数转成投票，先用一个具体卡池跑通。这样这个项目就从“产品创意”正式进入“算法设计”阶段。
```



卡池规则

可以。考虑到你是要直接拿去做 **MAGI 项目的规则数据库/规则引擎**，我建议不要写成面向玩家的攻略，而是写成**结构化规则文档**。

下面这版以目前的规则体系为基础，覆盖标准寻访、限定寻访、中坚寻访、联动/特殊寻访、保底、井、数据契约等。基础六星/五星/四星/三星概率分别为 2%/8%/50%/40%；限定寻访的 300 次额外领取、数据契约和 200 兑换等规则也纳入。([PRTS][1])

````md
# 明日方舟寻访系统规则

> 用于 MAGI 抽卡决策系统的卡池规则数据库与概率计算引擎。
>
> 注意：
> - 本文档用于程序建模，不作为游戏内最终规则说明。
> - 实际卡池规则以官方当期寻访公告为准。
> - 不同特殊寻访可能存在独立规则，因此规则引擎不得假设所有卡池完全相同。

---

# 1. 寻访基本稀有度概率

## 1.1 基础概率

| 稀有度 | 基础概率 |
|---|---:|
| 6★ | 2% |
| 5★ | 8% |
| 4★ | 50% |
| 3★ | 40% |

> 六星概率会受到六星保底概率提升机制影响。
>
> 基础概率目前适用于主要寻访类型，但特殊卡池可能存在额外规则。

---

# 2. 六星概率提升机制

## 2.1 六星保底

玩家在连续寻访中未获得 6★ 时：

- 每次未获得 6★ 的寻访都会增加六星概率。
- 达到对应保底次数后必定获得 6★。
- 一旦获得 6★，六星概率提升计数重置。

### 数据结构

```yaml
six_star_pity:
  current_count: 0
  base_rate: 0.02
  rate_increment: 0.02
  guaranteed_at: 100
````

> 具体概率递增规则应以当前游戏实际规则实现，不应直接硬编码在 UI 层。

---

# 3. 标准寻访

## 3.1 定义

标准寻访是明日方舟最基础的常规寻访类型。

标准寻访主要包含：

* 当前标准干员
* 其他仍处于标准寻访范围的干员
* 特定卡池 UP 干员

---

## 3.2 标准寻访特点

```yaml
banner_type: standard

limited_operator: false

has_spark: false

data_contract: false

six_star_pity:
  enabled: true

pity_carry_over:
  type: standard
```

---

## 3.3 标准寻访干员范围

由于干员数量不断增加，部分早期干员会从标准寻访范围移出并进入中坚寻访。

因此：

```text
标准寻访干员池
    ↓
随着版本更新
    ↓
部分早期干员移出
    ↓
进入中坚寻访
```

标准寻访干员池属于动态数据。

因此数据库应保存：

```yaml
operator_pool:
  start_date:
  end_date:
  operators:
```

而不是把干员池写死。

---

# 4. 限定寻访

限定寻访主要分为：

```text
限定寻访·春节
限定寻访·庆典
限定寻访·夏季
```

---

# 5. 限定寻访共同规则

## 5.1 限定干员

限定干员：

* 不会加入标准寻访
* 不会加入中坚寻访
* 通常仅在对应限定系列中再次出现
* 不同限定系列之间存在不同的复出规则

---

# 6. 限定寻访系列

## 6.1 春节限定

```yaml
series:
  name: spring_festival
  type: limited
```

通常在春节档期开放。

---

## 6.2 庆典限定

```yaml
series:
  name: celebration
  type: limited
```

主要对应：

* 周年庆典
* 半周年 / 感谢庆典等相关档期

---

## 6.3 夏季限定

```yaml
series:
  name: summer
  type: limited
```

通常在每年夏季活动期间开放。

---

# 7. 限定寻访 6★ 出率机制

限定寻访中的 6★ 出现后：

```text
6★
│
├── 当期 UP 6★
│
└── 其他 6★
```

具体权重根据当期限定寻访公告确定。

例如某庆典限定池可能：

```text
6★ 总概率
│
├── 当期限定 / 陪跑
│
└── 其他限定
```

部分较早的同系列限定干员会以特殊权重再次出现。

---

# 8. 限定寻访的特殊权重

同系列限定寻访中：

* 较新的若干限定干员可能获得特殊出率权重。
* 当获得的 6★ 不是当期出率提升干员时，部分历史限定干员可能获得 5 倍权重。

因此概率引擎需要支持：

```yaml
operator_weight:
  operator_id:
  weight:
```

例如：

```yaml
six_star_pool:
  - operator: current_limited
    weight: 70

  - operator: current_non_limited
    weight: 30

  - operator: old_limited_A
    weight: 5

  - operator: old_limited_B
    weight: 5
```

---

# 9. 限定寻访 300 抽机制

## 9.1 额外获得当期限定

限定寻访中：

```text
累计寻访 300 次
        ↓
额外获得一次当期限定干员
```

特点：

* 每期限定寻访最多触发一次
* 该奖励属于额外赠送
* 不属于普通寻访结果
* 不会产生寻访数据契约
* 不影响下一次 6★ 出现概率
* 计数仅绑定当前限定寻访
* 卡池结束后计数清零

---

## 9.2 数据结构

```yaml
limited_guarantee:
  enabled: true

  required_pulls: 300

  reward:
    type: current_limited_operator
    extra: true

  consumes_pity: false

  generates_data_contract: false

  carries_to_next_banner: false

  max_claims: 1
```

---

# 10. 限定寻访六星概率计数不跨池继承

限定寻访中的六星概率提升计数：

```text
限定池 A
    ↓
累计未获得 6★
    ↓
限定池结束
    ↓
计数清零
```

不会：

```text
限定池 A
    ↓
限定池 B
    ↓
继续累计
```

因此：

```yaml
pity_scope: banner
pity_carry_over: false
```

---

# 11. 寻访数据契约

限定寻访支持：

> 寻访数据契约

---

## 11.1 获取

每进行一次寻访：

```text
1 抽 → 1 张寻访数据契约
10 抽 → 10 张寻访数据契约
```

---

## 11.2 用途

数据契约可以用于：

> 当期寻访数据契约交换所

兑换指定干员。

---

# 12. 限定寻访 300 兑换

常规情况下：

```text
6★ → 300 寻访数据契约
5★ → 75 寻访数据契约
```

但部分满足解限条件的历史限定 6★：

```text
300 → 200
```

---

# 13. 200 数据契约兑换机制

部分历史限定干员达到对应解限年限后：

```text
兑换成本：

300 数据契约
        ↓
200 数据契约
```

因此数据库必须保存：

```yaml
operator_exchange:
  operator_id:
  normal_cost: 300
  reduced_cost: 200
  reduction_start:
  reduction_rule:
```

---

# 14. 数据契约过期

当当前限定寻访结束：

```text
未使用数据契约
        ↓
自动转换
        ↓
寻访参数模型
```

转换关系：

```text
1 数据契约
=
6 寻访参数模型
```

寻访参数模型不存在使用期限。

---

# 15. 限定干员解限时间

限定干员在首次出现后：

```text
限定池结束
      ↓
解限等待期
      ↓
未来限定池重新出现
```

在解限时间到来前：

```text
不能进入其他限定寻访
```

具体解限时间需要根据：

* 限定系列
* 干员首次出现时间
* 当前版本规则

动态计算。

---

# 16. 中坚寻访

中坚寻访用于承接部分已经从标准寻访移出的早期干员。

---

## 16.1 中坚寻访特点

```yaml
banner_type: kernel
```

主要用于：

* 早期 6★
* 早期 5★
* 中坚干员获取

---

## 16.2 中坚干员进入规则

当干员达到对应条件：

```text
标准寻访
    ↓
从标准寻访移出
    ↓
进入中坚寻访
```

移出批次按照干员实装时间动态确定。

---

# 17. 中坚寻访与标准寻访的区别

核心区别：

```text
标准寻访
    ↓
当前常规干员

中坚寻访
    ↓
已经从标准池移出的早期干员
```

因此程序必须区分：

```yaml
banner_type:
  - standard
  - kernel
```

---

# 18. 联合行动类寻访

联合行动属于特殊寻访。

典型特点：

```text
池内仅包含指定的若干 6★
池内仅包含指定的若干 5★
```

例如：

```yaml
banner_type: joint_action

six_star_pool:
  restricted: true

five_star_pool:
  restricted: true
```

因此：

> 联合行动不能简单套用普通标准池的干员池逻辑。

---

# 19. 定向甄选

定向甄选允许玩家：

```text
从指定干员列表
        ↓
选择若干干员
        ↓
成为出率提升对象
```

例如：

```yaml
selectable_six_star:
  count: 3

pool:
  restricted: true
```

因此概率引擎需要支持：

```text
玩家选择
    ↓
动态生成卡池概率
```

---

# 20. 联动寻访

联动寻访属于特殊卡池。

例如：

```text
明日方舟 × 其他 IP
```

其规则可能包括：

* 特殊限定干员
* 独立卡池
* 特殊保底
* 特殊获取方式
* 不进入标准寻访
* 不进入中坚寻访

因此：

```yaml
banner_type: collaboration

limited: true

standard_pool_join: false

kernel_pool_join: false
```

具体规则必须读取对应联动公告。

---

# 21. 特殊卡池

程序应支持：

```yaml
banner_type:
  standard
  limited
  kernel
  joint_action
  directional_selection
  collaboration
  special
```

不要假设未来所有卡池都属于上述类型。

---

# 22. 抽卡资源

寻访可以使用不同资源。

主要包括：

```text
合成玉
至纯源石
寻访凭证
十连寻访凭证
特殊寻访凭证
中坚寻访凭证
```

程序应统一转换为：

```text
available_pulls
```

同时保留资源来源。

---

# 23. 十连寻访

十连寻访：

```text
一次执行 10 次寻访
```

通常可以视为：

```text
10 × 单抽
```

但具体特殊池可能存在独立规则。

因此：

```yaml
multi_pull:
  count: 10
  behaves_as_single_pulls: true
```

---

# 24. 十连保底

部分寻访存在：

> 首个十连至少获得一个 5★ 或以上干员

因此需要记录：

```yaml
ten_pull_guarantee:
  enabled: true

  rarity: 5

  first_ten_only: true
```

该机制与六星概率保底不同。

---

# 25. 六星保底与五星保底必须分开

程序不得将两种机制混为一个 pity。

```text
6★ Pity
    ↓
控制六星概率

5★ Pity
    ↓
控制五 / 六星最低出现条件
```

数据结构：

```yaml
pity:
  six_star:
    current:
    reset_on:
    guarantee:

  five_star:
    current:
    guarantee:
```

---

# 26. 抽卡状态模型

玩家的卡池状态建议：

```yaml
player_banner_state:

  banner_id:

  pulls:

  six_star_pity:

  five_star_pity:

  limited_300_count:

  data_contract:

  resources:
```

---

# 27. 单次抽卡结果模型

```yaml
pull_result:

  rarity:

  operator_id:

  is_up:

  is_limited:

  is_target:

  generates_contract:

  consumes_pity:

  pity_after_pull:
```

---

# 28. 目标概率模型

MAGI 需要计算：

```text
P(目标在 N 抽内获得)
```

例如：

```yaml
target_probability:

  target:
    operator_id:

  pulls:

  probability:

  confidence:
```

---

# 29. 目标完成模型

玩家可能同时设置多个目标：

```yaml
targets:

  - id: limited
    priority: S

  - id: companion_6star
    priority: A

  - id: five_star
    priority: C
```

程序需要计算：

```text
P(完成目标 A)
P(完成目标 A+B)
P(完成目标 A+B+C)
```

---

# 30. 资源安全线

玩家可以设置：

```yaml
resource_safety:

  minimum_pulls: 60
```

例如：

```text
当前：
180 抽

安全线：
60 抽

最大可接受消耗：
120 抽
```

---

# 31. MAGI 输入

三个 MAGI 使用不同输入。

```yaml
magi_input:

  resources:
    available_pulls:

  banner:
    type:
    rules:

  targets:
    priority:

  player_preferences:

  player_box:

  resource_safety:
```

---

# 32. 大头输入

```yaml
big_head:

  current_pulls:

  six_star_pity:

  target_probability:

  expected_cost:

  guarantee_cost:

  spark_cost:

  resource_safety:

  banner_rules:
```

---

# 33. 小头输入

```yaml
small_head:

  operator_preference:

    art:
    chibi:
    story:
    character:
    voice:
    artist:
    fan_content:
    special_xp:
    collection:
```

---

# 34. 兔头输入

```yaml
rabbit_head:

  strength:

  afk:

  progression:

  big_numbers:

  special_mechanics:

  system_fit:

  roguelike:

  profession_team:

  future_potential:

  versatility:

  player_box:
```

---

# 35. MAGI 输出

```yaml
magi_result:

  big_head:
    decision:
    score:
    recommended_pulls:

  small_head:
    decision:
    score:
    recommended_pulls:

  rabbit_head:
    decision:
    score:
    recommended_pulls:

  final:
    decision:
    vote:
    recommended_pulls:
    stop_condition:
```

---

# 36. 投票规则

默认：

```text
3 个 MAGI
    ↓
2 / 3 多数决
```

结果：

```text
2 : 1 → 通过
3 : 0 → 强烈通过
1 : 2 → 不通过
0 : 3 → 强烈不通过
```

---

# 37. 高风险决策

如果：

```text
预计资源低于安全线
```

或者：

```text
预计消耗超过设定阈值
```

则提高投票门槛：

```text
普通决策：
2 / 3

高风险决策：
3 / 3
```

---

# 38. MAGI 不直接执行抽卡

MAGI 的职责：

```text
分析
 ↓
模拟
 ↓
投票
 ↓
提出建议
```

而不是：

```text
MAGI
 ↓
直接执行抽卡
```

最终决定权属于玩家。

---

# 39. 规则引擎设计原则

## 原则 1

卡池规则与 MAGI 分离。

---

## 原则 2

卡池数据与规则代码分离。

---

## 原则 3

概率计算与 UI 分离。

---

## 原则 4

所有概率计算必须可复现。

---

## 原则 5

特殊卡池必须允许独立规则。

---

## 原则 6

未来规则变化时尽可能只修改数据库，而不是修改 MAGI。

---

# 40. 推荐数据库结构

```text
Banner
├── banner_id
├── banner_type
├── series
├── start_time
├── end_time
├── ruleset_id
└── operator_pool

BannerRule
├── ruleset_id
├── six_star_rate
├── five_star_rate
├── four_star_rate
├── three_star_rate
├── six_star_pity
├── five_star_guarantee
├── spark_enabled
├── spark_count
├── data_contract_enabled
└── special_rules

Operator
├── operator_id
├── name
├── rarity
├── profession
├── limited
├── limited_series
└── release_date

BannerOperator
├── banner_id
├── operator_id
├── rate_weight
├── is_up
└── exchange_cost

Player
├── player_id
├── resources
├── preferences
├── safety_line
└── settings

PlayerOperator
├── player_id
├── operator_id
└── owned

Decision
├── player_id
├── banner_id
├── resources
├── targets
├── big_head_result
├── small_head_result
├── rabbit_head_result
└── final_result
```

---

# 41. 最终系统流程

```text
玩家进入网站
        ↓
选择卡池
        ↓
读取卡池规则
        ↓
读取卡池干员
        ↓
输入玩家资源
        ↓
输入当前保底
        ↓
选择抽卡目标
        ↓
设置目标优先级
        ↓
设置资源安全线
        ↓
输入角色偏好
        ↓
输入强度需求
        ↓
读取玩家 BOX
        ↓
概率模拟
        ↓
┌───────────────┐
│               │
│   🧠 大头      │
│   ❤️ 小头      │
│   🐇 兔头      │
│               │
└───────┬───────┘
        ↓
MAGI 投票
        ↓
抽 / 不抽 / 抽多少
        ↓
给出停止条件
        ↓
玩家决定是否执行
        ↓
记录实际结果
```

---

# 42. 未来扩展

未来可以增加：

```text
长期资源规划
多卡池竞争
机会成本
未来卡池预测
玩家行为学习
个性化权重
强化学习
LLM 决策解释
```

但这些都不应影响核心规则引擎。

---

# 43. 核心原则

> **卡池规则负责描述游戏。**
>
> **概率引擎负责描述不确定性。**
>
> **三个 MAGI 负责描述不同目标。**
>
> **投票器负责产生最终建议。**
>
> **玩家负责做最终决定。**

```

**一个需要特别注意的地方**：上面把“六星概率提升”“十连保底”等写成了规则框架，但如果你准备把它直接作为程序的 `rules.md`，建议下一步把每一种卡池的**精确概率递增公式、UP 权重、计数继承规则**逐项核实后再固化进数据库。尤其是限定池的不同系列、联动池和特殊池，不能简单套用同一套规则。当前官方公告也明确展示了不同限定池的具体权重配置，例如 2026 年庆典限定池的当期 6★ 与历史限定采用不同权重。:contentReference[oaicite:1]{index=1}

如果你是准备直接放进 GitHub，我建议这个文件命名为 **`docs/game-rules/gacha-rules.md`**，而不是放在项目根目录；以后再拆成 `standard.md`、`limited.md`、`kernel.md`、`collaboration.md`，程序实现时会更舒服。
```

[1]: https://prts.wiki/w/%E5%AF%BB%E8%AE%BF%E8%A7%84%E5%88%99?utm_source=chatgpt.com "寻访规则 - PRTS - 玩家共同构筑的明日方舟中文Wiki"

---

# 44. 交互设计决策（v0.2）

> 以下为 MAGI 页面在实现过程中与用户确认后的交互决策，任何后续改动需保持与本文一致。

## 44.1 维度支持正负双向打分（加分 + 减分）

- 小头 / 兔头的已选维度卡片中加入**分隔线**：分隔线之前的维度为**正向（加分）**，分隔线之后的维度为**负向（减分）**。
- 排序仍是「该维度对用户的重要程度」；方向（正/负）由卡片相对分隔线的位置决定。
- 交互方式：
  - 卡片上的「加分 / 减分」标签可一键切换方向；
  - 拖动卡片跨越分隔线会自动切换方向；
  - 拖动卡片拖过分隔线区域（`onSeparatorOver`）即切换为负向并移入负向区末尾。
- 引擎语义（`smallHeadEngine.js` / `rabbitHeadEngine.js`）：
  - 正向与负向分区**各自独立归一化**：`净分 = 正向加权均分 − 负向加权均分`；
  - 负向维度中打分越高，扣分越多（用户明确反感的维度，角色在该维度越强/越贴合反而越减分）；
  - 负向维度得分 ≥ 8 时计入「减分项 / 扣分项」理由。
- 权重计算：正负分区内各自从 rank 1 开始计算权重（rank 1 → ×1.5，线性递减至 ×1.0），负向权重取负值。

## 44.2 抽卡预算直接沿用攒抽计算器

- MAGI 页面**不再提供预算编辑输入**，预算 = 攒抽计算器的计算结果，用户无需再次填写。
- 实现：攒抽计算器在每次 `gachaResourcesCalculation()` 后将结果写入 `localStorage["LastDrawResult"]`（含 `totalDraw`、各组成分项、`scheduleName`、`scheduleEnd`、`savedAt`）。
- MAGI 页面挂载时读取 `LastDrawResult` 并展示：
  - 预计总抽数 + 是否够井；
  - 来源（排期名称 / 截止时间 / 计算时间）；
  - 组成分项 chips（现有 / 日常 / 潜在 / 氪金 / 活动 / 其他 / 搓玉）。
- 若尚未在攒抽计算器计算过：显示提示并提供「前往攒抽计算器」跳转按钮。
- 提供「重新读取」按钮用于刷新预算。

## 44.3 大头 = 抽数预算 + 抽卡目标（②③合并）

- 原「② 抽数预算」与「③ 抽卡目标」合并为**一个「② 大头：理性决策（🧠）」板块**。
- 板块内容：抽卡预算（自动沿用）→ 抽卡目标（拖动卡片）→ 兜底约束（资源安全线 + 概率阈值）。
- 页面编号随之调整为：① 卡池 / ② 大头 / ③ 小头 / ④ 兔头 / ⑤ 三头决议。

## 44.4 抽卡目标也用拖动卡片表示

- 抽卡目标与维度卡片一致，采用「候选池 + 排序池」双池拖动卡片交互。
- 候选池 = 目标类型（获得限定 / 获得限定+陪跑 / 往期限定 / 五星 / 常驻六星 / 自定义），**允许重复添加**（同一类型可为多个干员分别设立目标）。
- 排序池内**拖动排序 = 目标优先级**（越靠前越优先保障，替代原先的"级别第一优先级"排序）。
- 卡片内容：级别标签（S/A/B/C 标记）、目标类型、干员名输入、达成概率 / 所需抽数（或自定义所需抽数）、井提示。
- 拖回候选池或点 ✕ 即移除目标。

## 44.5 条件卡片内部打分（排序 = 重要程度，卡片内分数 = 具体打分）

- **排序只表明该维度对用户的重要程度（权重）**；
- **卡片内部的分数表示用户对该维度、对该目标干员的具体打分**（0~10，小头 = 偏好分，兔头 = 强度分）。
- 每张已选维度卡片内部按目标干员展示一行小滑块（每个可打分目标一行），不再使用独立的折叠面板打分区。
- 未填写干员名的目标不计入可打分目标；未选中任何目标时卡片内提示先到「② 大头」添加目标。

---

# 45. 交互设计决策（v0.3）

> 在 v0.2 基础上的第二轮交互调整。

## 45.1 预算组成分项向下取整

- 「② 大头」预算组成分项 chips（现有 / 日常 / 潜在 / 氪金 / 活动 / 其他 / 搓玉）展示时**向下取整**（如 现有 45.6 → 45）；
- 取整后为 0 的分项自动隐藏。

## 45.2 候选 / 已选维度改为左右布局

- 小头 / 兔头的维度池从「上下两栏」改为**左右布局**：左侧为候选维度（竖排卡片），右侧为已选维度（上下堆叠）；
- 拖动仍双向生效：候选拖入右侧 = 选中，已选拖回左侧 / 点 ✕ = 移除；
- 移动端（≤640px）自动回退为上下堆叠；
- 目标池保持上下布局（目标卡片内容较宽）。

## 45.3 负分分隔线默认常驻

- 分隔线不再只在存在负向维度时才出现，而是**默认一直存在**：
  - 全为正向时，分隔线常驻在已选列表**末尾**；
  - 存在负向维度时，分隔线渲染在**第一张负向卡片之前**；
  - 空态时同样显示分隔线作为负向区的视觉引导；
- 拖动卡片经过分隔线仍会自动切换为负向（减分）。

## 45.4 默认不选择任何条件卡片

- 小头 / 兔头的已选维度**默认全部为空**，所有维度初始都在候选池，由用户自行拖入；
- 引擎增加空维度守卫：未选择任何维度时，小头 / 兔头返回「未选择任何偏好/强度维度，请拖入维度卡片并打分」的提示，而不是产生无效的 0 分决议。

## 45.5 条件卡片内部打分滑块

- 每张已选条件卡片内部常驻打分滑块（每个已填干员名的目标一行，0~10），卡片外不再有独立打分入口。
